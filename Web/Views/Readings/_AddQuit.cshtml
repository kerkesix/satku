@model dynamic
<div id="addQuit" class="modal" role="dialog" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <h4><i class="fa fa-ambulance fa-2x"></i> <span data-bind="text: Ksx.ReferenceData.attendeesMap[id]"></span></h4>
            </div>

            <div class="modal-body">
                <p>
                    Keskeytä kävelijän matkanteko. Jos kävelijä on kävellyt omin jaloin huoltopisteelle, luetaan 
                    kävelijä sisään ja sen jälkeen keskeytetään. Jos kävelijä on keskeyttänyt huoltopisteiden välillä, 
                    merkataan hänet keskeyttäneeksi viimeksi käydyllä huoltopisteellä, ja lisäksi tallennetaan sen 
                    jälkeen kävellyt kilometrit tilastoja varten.
                </p>
                <form data-bind="submit: addQuit.bind($data)">
                    <div class="form-group">
                        <label for="time">Aika mmuodossa tt.mm</label>
                        <input type="text" class="form-control" style="width: 100px;" placeholder="tt.mm" required="required" name="time"
                               title="Anna aika muodossa tt.mm" pattern="\d{1,2}[.:]\d{1,2}" data-bind="value: addQuitTime" />
                    </div>
                    
                    @*Do not show walked since last if quit is added during stay at the checkpoint*@
                    <div class="form-group" data-bind="visible: dateTimeOut">
                        <label for="walkedSinceLast">Matka edelliseltä huoltopisteeltä (jos haettiin matkalta), km</label>
                        <input type="text" class="form-control" style="width: 100px;" name="walkedSinceLast" required pattern="\d{1,2}([,]\d{1,2})?"
                               data-bind="value: addQuitWalkedSinceLast" />
                    </div>
                    
                    <div class="form-group">
                        <label for="description">Selite (ei pakollinen)</label>
                        <textarea name="description" class="form-control" data-bind="value: addQuitDescription"></textarea>
                    </div>

                    <p>
                        <button type="submit" class="btn btn-primary" style="margin-right: 10px;"><i class="fa fa-ambulance"></i> Lisää keskeytys</button>
                        <button type="button" class="btn btn-default" data-dismiss="modal">Peru</button>
                    </p>
                </form>
            </div>
        </div>
    </div>
</div>